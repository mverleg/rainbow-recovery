
In the main menu, hovering monsters with mouse does not show click cursor. It should show when hovering monsters but not at other times.

Red level tries to load “http://localhost:3000/red/shared.js” but it should be “http://localhost:3000/shared.js” (parent)

Pressing ESC in a level should bring us back to the menu

---

The requested module 'http://localhost:3080/red/red.js' doesn't provide an export named: 'isSolidCellLocal'
